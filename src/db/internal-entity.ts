/**
 * The internal Earthy entity used to back Earthy-owned inventory (villas with
 * no external host). A single deterministic id so the same booking / payment /
 * ledger architecture applies to owned properties. Phase 1B.5.
 */
export const INTERNAL_EARTHY_USER_ID = "usr_earthy_internal";
